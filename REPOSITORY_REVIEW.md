# Repository Review

Reviewed: 2026-07-14  
Commit: `acb8c3e`

The scaffold is directionally sound, but feature work should pause for a short foundation pass. TypeScript and packaging checks pass, yet there are concurrency, performance, verification, reusable webview infrastructure, state-management, and distribution issues worth fixing now.

## Prioritized findings

| #   | Finding and correction                                                                                                                                                                                                                                                                                                                                                   | Category                   | Impact                 | Effort | Fix risk | Confidence | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------- | ---------------------- | ------ | -------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Completed 2026-07-14.** `npm run check` now enforces formatting, linting, unit tests, typechecking, a production build, a VS Code 1.92 Extension Host smoke test, and package enumeration. GitHub Actions runs the same gate under Xvfb.                                                                                                                               | Tests / DX                 | High                   | M      | Low      | High       | [`package.json`](package.json), [`reviewProtocol.test.ts`](src/common/reviewProtocol.test.ts), [`index.cjs`](test/integration/index.cjs), [`ci.yml`](.github/workflows/ci.yml)                                                                                                                                                                                                                                                                                                                                    |
| 2   | Introduce a generic webview base class so future webviews do not repeat lifecycle and security boilerplate. It should own webview options, local resource roots, CSP/nonce HTML generation, resource URI creation, JSON-RPC connection setup, visibility refresh hooks, and disposal. Domain-specific state and RPC handlers must remain in subclasses or collaborators. | Architecture / DX          | High                   | M      | Medium   | High       | These responsibilities are currently coupled to the review feature at [`reviewPanelService.ts:89`](src/webviewPanel/reviewPanelService.ts#L89), [`reviewPanelService.ts:215`](src/webviewPanel/reviewPanelService.ts#L215), and [`webviewRpc.ts:13`](src/webviewPanel/webviewRpc.ts#L13).                                                                                                                                                                                                                         |
| 3   | Establish deliberate webview state management with a clear split between host-authoritative domain state and ephemeral UI state. Serialize persisted note/session mutations, version the persisted schema, restore drafts and UI state with `getState`/`setState`, and add a monotonically increasing state revision so stale notifications cannot replace newer state.  | Correctness / Architecture | High                   | M      | Medium   | High       | Note operations currently use uncoordinated read-modify-write updates at [`reviewPanelService.ts:130`](src/webviewPanel/reviewPanelService.ts#L130), [`reviewPanelService.ts:149`](src/webviewPanel/reviewPanelService.ts#L149), and [`reviewPanelService.ts:207`](src/webviewPanel/reviewPanelService.ts#L207); UI state is split across local hooks at [`App.tsx:16`](src/webview/reviewPanel/App.tsx#L16), while the persistence API at [`vscodeApi.ts:3`](src/webview/reviewPanel/vscodeApi.ts#L3) is unused. |
| 4   | Coalesce and sequence state publication. Cursor movement currently starts a full asynchronous snapshot—including a Git subprocess and command enumeration—on every event; overlapping snapshots can arrive out of order. Use a debounced or single-flight refresh path, cache stable metadata, and discard superseded work.                                              | Correctness / Performance  | High                   | M      | Medium   | High       | [`reviewPanelService.ts:51`](src/webviewPanel/reviewPanelService.ts#L51), [`reviewPanelService.ts:163`](src/webviewPanel/reviewPanelService.ts#L163), [`reviewPanelService.ts:245`](src/webviewPanel/reviewPanelService.ts#L245), [`reviewPanelService.ts:285`](src/webviewPanel/reviewPanelService.ts#L285)                                                                                                                                                                                                      |
| 5   | Add verified third-party license notices to the packaged extension. Eleven copied Microsoft files reference a missing root license, while the bundled dependencies' notices are also absent from the five packaged files.                                                                                                                                                | Distribution / Compliance  | High before publishing | S      | Low      | High       | [`services.ts:1`](src/util/common/services.ts#L1), [`package.json:8`](package.json#L8), [`.vscodeignore:11`](.vscodeignore#L11). Microsoft's MIT license requires its notice to accompany substantial copies: [official VS Code license](https://github.com/microsoft/vscode/blob/main/LICENSE.txt).                                                                                                                                                                                                              |
| 6   | Raise the minimum supported VS Code API from `^1.92.0` to `^1.125.0` and keep `@types/vscode` aligned with that floor. Document the support policy and add an extension-host smoke test against the minimum version so future type/API upgrades are deliberate.                                                                                                          | Dependencies / Correctness | Medium                 | S      | Low      | High       | [`package.json:13`](package.json#L13), [`package.json:62`](package.json#L62). VS Code documents `engines.vscode` as the minimum API and `@types/vscode` as the API declaration in [Extension Anatomy](https://code.visualstudio.com/api/get-started/extension-anatomy); [VS Code 1.125](https://code.visualstudio.com/updates/v1_125) is a stable release matching the currently installed type definitions.                                                                                                      |
| 7   | Add explicit loading, retry, and actionable error states for every RPC operation. Refresh, add, and delete failures currently become rejected click-handler promises with no visible recovery path; this work should consume the state architecture from finding #3 rather than create another independent state mechanism.                                              | Correctness / UX           | Medium                 | S      | Low      | High       | [`App.tsx:55`](src/webview/reviewPanel/App.tsx#L55), [`App.tsx:63`](src/webview/reviewPanel/App.tsx#L63), [`App.tsx:77`](src/webview/reviewPanel/App.tsx#L77)                                                                                                                                                                                                                                                                                                                                                     |
| 8   | Upgrade and re-audit the packaging toolchain. The full audit reports two high and two moderate development-only advisories; production dependencies are clean.                                                                                                                                                                                                           | Security / Dependencies    | Medium                 | S      | Medium   | High       | [`package.json:63`](package.json#L63). The high findings are quadratic-complexity issues in transitive Markdown processing: [linkify-it advisory](https://github.com/advisories/GHSA-22p9-wv53-3rq4), [markdown-it advisory](https://github.com/advisories/GHSA-6v5v-wf23-fmfq).                                                                                                                                                                                                                                  |

Recommended landing order: #1 is complete. Design #2 and #3 together, then land the generic webview lifecycle before migrating review state; address #4 with regression tests; #6–#8 can then land independently. The license work in #5 must precede any public distribution.

## What is already good

- Strict TypeScript is enabled and `npm run check-types` passes.
- The webview uses a restrictive CSP, local resource roots, React-escaped content, and fixed-argument `execFile`; no credible command-injection, XSS, or credential issue was found.
- `npm audit --omit=dev` reports zero production vulnerabilities.
- `vsce ls` contains the expected runtime files, and the worktree was clean before this report was added.

## Maintainer architecture decisions

### Retain the VS Code dependency-injection framework

The earlier recommendation to remove the copied VS Code dependency-injection framework has been withdrawn. The framework is an intentional architectural choice and must be preserved by future implementation plans.

Near-term work around the DI code should be limited to tests, license compliance, documenting its upstream provenance, and defining an update strategy. Extracting it into a standalone npm package is a possible future project and should receive its own API, packaging, compatibility, and release plan rather than being folded into extension cleanup.

## Product-direction decisions to make soon

### Introduce an explicit review-session model

The README promises session detection, but storage is currently a flat note array with transient branch/file context. Capturing repository identity, revision/base, and stable source anchors now avoids a difficult persistence migration later.

### Define an agent-adapter boundary

Define this boundary before enabling **Send bundle**. Agent detection and external command IDs are currently embedded in the panel service, while the UI already anticipates multiple targets. A small capability/handoff interface would isolate Codex and Copilot contract changes.

## Verification performed

- `npm run check` — passed on 2026-07-14.
- Formatting and ESLint — passed with zero warnings.
- Vitest — seven unit cases passed.
- VS Code 1.92 Extension Host — extension activation and public command registration passed.
- TypeScript and the production build — passed.
- `vsce ls` — passed and listed the expected five runtime files.
- `npm audit --omit=dev --json` — zero production vulnerabilities.
- Full `npm audit --json` — two high and two moderate development-tooling advisories.

The VS Code 1.92 smoke run reports that the secondary-sidebar view container is not recognized and falls back to Explorer. The smoke test intentionally remains pinned to the declared minimum until finding #6 raises the API floor; that finding should update the test version in the same change.

## Audit scope and limitations

All tracked source and configuration files were reviewed. The package lock was assessed through npm's dependency and audit tooling rather than line by line. The Extension Development Host was not launched manually, and generated assets were not rebuilt during the read-only audit.

## Recommended planning scope

Each numbered finding is deliberately separable so it can be reviewed and implemented one at a time. With #1 complete, prepare plans for #2–#4 in that order while treating #2 and #3 as one coordinated design boundary. No plan should remove or replace the VS Code dependency-injection framework. Findings #5–#8 can follow as independent compliance, compatibility, UX, and dependency-maintenance passes.
